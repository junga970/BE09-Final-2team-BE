apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
  namespace: momnect
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chat-service
  template:
    metadata:
      labels:
        app: chat-service
    spec:
      containers:
        - name: chat-service
          image: junga970/chat-service:dev-1
          envFrom:
            - secretRef:
                name: app-secret
---
apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: momnect
spec:
  selector:
    app: chat-service
  clusterIP: None   # Headless (Eureka 의존)
  ports:
    - port: 80
      targetPort: 80   # placeholder
